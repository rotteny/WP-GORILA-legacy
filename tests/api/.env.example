# Copie pra tests/api/.env e ajuste os valores.
# tests/api/.env é ignorado pelo git.

BASE_URL=http://wp.local
LOGIN_EMAIL=admin@gorila.com
LOGIN_PASSWORD=gorila@2025

# Instância CONNECTED de onde sai a mensagem.
SOURCE_SLUG=testehelio

# Número destino (cobaia). DDI+DDD+número, sem +/espaços/hífens.
DEST_NUMBER=

# Marca todas as mensagens de teste com este prefixo pra você reconhecer no WhatsApp.
TEST_TAG=[wp-gorila-smoke]
