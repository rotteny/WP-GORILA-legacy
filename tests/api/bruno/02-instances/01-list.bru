meta {
  name: 01 - GET /instances (listar)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/whatsapp/instances
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("status 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
  test("é um array", function () {
    expect(res.getBody()).to.be.an("array");
  });
}
