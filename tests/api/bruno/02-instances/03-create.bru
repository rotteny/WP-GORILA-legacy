meta {
  name: 03 - POST /instances (criar)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/whatsapp/instances
  body: json
  auth: none
}

headers {
  Accept: application/json
}

body:json {
  {
    "slug": "bruno-temp",
    "name": "Bruno Temp"
  }
}

tests {
  test("status 2xx", function () {
    expect(res.getStatus()).to.be.within(200, 299);
  });
}
