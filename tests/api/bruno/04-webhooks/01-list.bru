meta {
  name: 01 - GET /instances/{slug}/webhooks
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/whatsapp/instances/{{slug}}/webhooks
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("status 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
